/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  /*images: {
    loader: "imgix",
    path: "https://alphadune.lember.com.ua/",
  },*/
};

module.exports = nextConfig;
